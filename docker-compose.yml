version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: pulpapp-db
    environment:
      POSTGRES_DB: pulpapp_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
    ports:
      - "5433:5432"